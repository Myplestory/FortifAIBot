DISCORD_BOT_TOKEN=
ANTHROPIC_API_KEY=
MODEL_GENERATE=claude-opus-4-7
MODEL_REFINE=claude-sonnet-4-6
# Optional. Set to a guild (server) id to enable instant slash-command sync
# in that guild. Without this, schema changes propagate via Discord's global
# command tree which can take up to ~1 hour.
DEV_GUILD_ID=
